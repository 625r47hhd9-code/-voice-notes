Голосовой ассистент v7.6

Исправлено обновление приложения:
- видимый значок v7.6 встроен прямо в HTML;
- HTML загружается сначала из сети, а не из старого кэша;
- старые кэши удаляются при активации нового service worker;
- service worker сразу активируется через skipWaiting;
- приложение принудительно проверяет обновление при запуске;
- после смены service worker страница автоматически перезагружается.

Если после загрузки в GitHub виден значок v7.6 — открыта актуальная версия.
